# OpenTelemetry Demo App Configuration
# This file contains default configuration values that can be overridden by CLI arguments

# Redis connection URL
redis_url: "redis://127.0.0.1:6379"

# Server listen address
listen_address: "127.0.0.1:8080"
